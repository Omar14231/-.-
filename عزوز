-- Services
local Players = game:GetService("Players")
local TweenService = game:GetService("TweenService")
local RunService = game:GetService("RunService")

-- التأكد من أن السكريبت يعمل على جانب العميل فقط
if not RunService:IsClient() then
	return
end

-- الحصول على اللاعب المحلي
local player = Players.LocalPlayer
local playerGui = player:WaitForChild("PlayerGui")

-- إنشاء الشاشة الرئيسية
local screenGui = Instance.new("ScreenGui")
screenGui.Name = "IntroScreen"
screenGui.ResetOnSpawn = false
screenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
screenGui.IgnoreGuiInset = true -- لتغطية الشاشة بالكامل

-- إناء الشاشة السوداء (مبدئياً شفاف)
local blackFrame = Instance.new("Frame")
blackFrame.Name = "BlackFrame"
blackFrame.Size = UDim2.new(1, 0, 1, 0)
blackFrame.Position = UDim2.new(0, 0, 0, 0)
blackFrame.BackgroundColor3 = Color3.new(0, 0, 0)
blackFrame.BackgroundTransparency = 1 -- تبدأ شفافة
blackFrame.BorderSizePixel = 0
blackFrame.Parent = screenGui

-- النص في المنتصف
local centerText = Instance.new("TextLabel")
centerText.Name = "CenterText"
centerText.Size = UDim2.new(0.6, 0, 0.1, 0)
centerText.Position = UDim2.new(0.2, 0, 0.45, 0)
centerText.BackgroundTransparency = 1
centerText.Text = "Script RS"
centerText.TextColor3 = Color3.new(1, 1, 1)
centerText.TextTransparency = 1 -- تبدأ شفافة
centerText.TextScaled = true
centerText.Font = Enum.Font.GothamBold
centerText.Parent = blackFrame

-- النص على اليمين (أسفل)
local rightText = Instance.new("TextLabel")
rightText.Name = "RightText"
rightText.Size = UDim2.new(0.3, 0, 0.05, 0)
rightText.Position = UDim2.new(0.7, 0, 0.9, 0)
rightText.AnchorPoint = Vector2.new(0.5, 0.5)
rightText.BackgroundTransparency = 1
rightText.Text = "عربي"
rightText.TextColor3 = Color3.new(1, 1, 1)
rightText.TextTransparency = 1 -- تبدأ شفافة
rightText.TextScaled = true
rightText.Font = Enum.Font.Gotham
rightText.TextXAlignment = Enum.TextXAlignment.Right
rightText.Parent = blackFrame

-- النص على اليسار (أسفل)
local leftText = Instance.new("TextLabel")
leftText.Name = "LeftText"
leftText.Size = UDim2.new(0.3, 0, 0.05, 0)
leftText.Position = UDim2.new(0.3, 0, 0.9, 0)
leftText.AnchorPoint = Vector2.new(0.5, 0.5)
leftText.BackgroundTransparency = 1
leftText.Text = "1.0v"
leftText.TextColor3 = Color3.new(1, 1, 1)
leftText.TextTransparency = 1 -- تبدأ شفافة
leftText.TextScaled = true
leftText.Font = Enum.Font.Gotham
leftText.TextXAlignment = Enum.TextXAlignment.Left
leftText.Parent = blackFrame

-- إضافة الشاشة إلى واجهة اللاعب
screenGui.Parent = playerGui

-- وظيفة للتحويلات
local function createTween(object, properties, duration)
	local tweenInfo = TweenInfo.new(
		duration,
		Enum.EasingStyle.Quad,
		Enum.EasingDirection.InOut
	)
	
	local tween = TweenService:Create(object, tweenInfo, properties)
	return tween
end

-- الانتظار قليلاً قبل البدء
wait(0.5)

-- 1. ظهور الشاشة السوداء بتدريج (حبة حبة)
local fadeInTween = createTween(blackFrame, {BackgroundTransparency = 0}, 1.5)
fadeInTween:Play()

-- الانتظار حتى تكتمل الشاشة السوداء
wait(1.5)

-- 2. ظهور النصوص بعد اكتمال الشاشة السوداء
local textFadeDuration = 0.8

-- ظهور النص في المنتصف
local centerTextTween = createTween(centerText, {TextTransparency = 0}, textFadeDuration)
centerTextTween:Play()

-- ظهور النص على اليمين
local rightTextTween = createTween(rightText, {TextTransparency = 0}, textFadeDuration)
rightTextTween:Play()

-- ظهور النص على اليسار
local leftTextTween = createTween(leftText, {TextTransparency = 0}, textFadeDuration)
leftTextTween:Play()

-- الانتظار قليلاً بعد ظهور النصوص
wait(2)

-- 3. اختفاء النصوص أولاً
local fadeOutTextDuration = 1

-- اختفاء النص في المنتصف
local centerTextOutTween = createTween(centerText, {TextTransparency = 1}, fadeOutTextDuration)
centerTextOutTween:Play()

-- اختفاء النص على اليمين
local rightTextOutTween = createTween(rightText, {TextTransparency = 1}, fadeOutTextDuration)
rightTextOutTween:Play()

-- اختفاء النص على اليسار
local leftTextOutTween = createTween(leftText, {TextTransparency = 1}, fadeOutTextDuration)
leftTextOutTween:Play()

-- الانتظار حتى تختفي النصوص
wait(fadeOutTextDuration)

-- 4. اختفاء الشاشة السوداء
local fadeOutTween = createTween(blackFrame, {BackgroundTransparency = 1}, 1.5)
fadeOutTween:Play()

-- الانتظار حتى تختفي الشاشة
wait(1.5)

-- 5. عرض الشاشة الثانية (مرحبا بك)
local secondBlackFrame = Instance.new("Frame")
secondBlackFrame.Name = "SecondBlackFrame"
secondBlackFrame.Size = UDim2.new(1, 0, 1, 0)
secondBlackFrame.Position = UDim2.new(0, 0, 0, 0)
secondBlackFrame.BackgroundColor3 = Color3.new(0, 0, 0)
secondBlackFrame.BackgroundTransparency = 1 -- تبدأ شفافة
secondBlackFrame.BorderSizePixel = 0
secondBlackFrame.Parent = screenGui

-- النص الترحيبي
local welcomeText = Instance.new("TextLabel")
welcomeText.Name = "WelcomeText"
welcomeText.Size = UDim2.new(0.8, 0, 0.15, 0)
welcomeText.Position = UDim2.new(0.1, 0, 0.4, 0)
welcomeText.BackgroundTransparency = 1
welcomeText.Text = "مرحبا بك في سكريبت RS 👋"
welcomeText.TextColor3 = Color3.new(1, 1, 1)
welcomeText.TextTransparency = 1 -- تبدأ شفافة
welcomeText.TextScaled = true
welcomeText.Font = Enum.Font.GothamBold
welcomeText.Parent = secondBlackFrame

-- رابط الديسكورد
local discordLink = Instance.new("TextLabel")
discordLink.Name = "DiscordLink"
discordLink.Size = UDim2.new(0.9, 0, 0.1, 0)
discordLink.Position = UDim2.new(0.05, 0, 0.6, 0)
discordLink.BackgroundTransparency = 1
discordLink.Text = "https://discord.gg/NCmesk7RbX"
discordLink.TextColor3 = Color3.new(0, 0.6, 1)
discordLink.TextTransparency = 1 -- تبدأ شفافة
discordLink.TextScaled = true
discordLink.Font = Enum.Font.Gotham
discordLink.Parent = secondBlackFrame

-- 6. ظهور الشاشة الثانية بتدريج
wait(0.3)

-- ظهور الخلفية السوداء للشاشة الثانية
local secondFadeIn = createTween(secondBlackFrame, {BackgroundTransparency = 0}, 1)
secondFadeIn:Play()

wait(1)

-- ظهور النص الترحيبي
local welcomeFadeIn = createTween(welcomeText, {TextTransparency = 0}, 0.8)
welcomeFadeIn:Play()

wait(0.8)

-- ظهور رابط الديسكورد
local discordFadeIn = createTween(discordLink, {TextTransparency = 0}, 0.8)
discordFadeIn:Play()

-- نسخ الرابط تلقائياً
wait(1)

if setclipboard then
	setclipboard("https://discord.gg/NCmesk7RbX")
	
	-- إضافة رسالة تأكيد
	local copyMessage = Instance.new("TextLabel")
	copyMessage.Name = "CopyMessage"
	copyMessage.Size = UDim2.new(0.8, 0, 0.08, 0)
	copyMessage.Position = UDim2.new(0.1, 0, 0.7, 0)
	copyMessage.BackgroundTransparency = 1
	copyMessage.Text = "تم النسخ إلى الحافظة!"
	copyMessage.TextColor3 = Color3.new(0, 1, 0)
	copyMessage.TextTransparency = 1
	copyMessage.TextScaled = true
	copyMessage.Font = Enum.Font.Gotham
	copyMessage.Parent = secondBlackFrame
	
	local copyFadeIn = createTween(copyMessage, {TextTransparency = 0}, 0.5)
	copyFadeIn:Play()
end

-- الانتظار قليلاً ثم اختفاء كل شيء
wait(3)

-- اختفاء النصوص أولاً
local textFadeOut = createTween(welcomeText, {TextTransparency = 1}, 1)
textFadeOut:Play()

local discordFadeOut = createTween(discordLink, {TextTransparency = 1}, 1)
discordFadeOut:Play()

wait(1)

-- اختفاء الخلفية السوداء
local secondFadeOut = createTween(secondBlackFrame, {BackgroundTransparency = 1}, 1.5)
secondFadeOut:Play()

wait(2)

-- إزالة كل شيء
screenGui:Destroy()

print("اكتمل العرض!")
